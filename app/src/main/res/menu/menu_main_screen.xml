<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainScreen">
    <item android:id="@+id/action_add"
        android:title="Add Event"
        android:onClick="addEventSelected"
        app:showAsAction="always"/>
    <item android:id="@+id/action_remove"
        android:title="Remove All Events"
        android:onClick="removeEvents"
        app:showAsAction="never"/>
</menu>
